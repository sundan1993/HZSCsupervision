$.position = function(){
	// var e = event || window.event;
	// var parentX = $('#'+ parentId).offset().left, parentY = $('#'+ parentId).offset().top;
	// var childX = event.pageX, childY = event.pageY;
	// var x = childX - parentX, y = childY - parentY;
	// console.log('left: '+ x +'px; top: ' + y + 'px');
	// var posArr = $('.map .map__icon').map(function(i, item){
	// 	var parentX = $('#map').offset().left, parentY = $('#map').offset().top;
	// 	var childX = $(item).offset().left, childY = $(item).offset().top;
	// 	var x = childX - parentX, y = childY - parentY;
	// 	return x;
	// })
	// if(posArr.length > 100){
	// 	console.log(posArr.slice(0,100));
	// 	console.log(posArr.slice(100));
	// } else {
	// 	console.log(posArr);
	// }
}
$.position();